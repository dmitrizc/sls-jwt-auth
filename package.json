{
  "name": "sls-jwt-auth",
  "description": "",
  "version": "0.1.0",
  "scripts": {
    "start": "NODE_ENV=local sls offline start -t -s local",
    "start:dev": "NODE_ENV=dev sls offline start -t -s dev",
    "db:migrate": "NODE_ENV=local npx sequelize db:migrate",
    "db:migrate:dev": "NODE_ENV=dev npx sequelize db:migrate",
    "db:migrate:prod": "NODE_ENV=prod npx sequelize db:migrate",
    "deploy:dev": "NODE_ENV=dev sls deploy -s dev -v",
    "deploy:prod": "NODE_ENV=prod sls deploy -s prod -v"
  },
  "dependencies": {
    "await-to-js": "^2.1.1",
    "bcryptjs": "^2.4.3",
    "dotenv-flow": "^3.1.0",
    "http-status": "^1.3.2",
    "joi": "^14.3.1",
    "jsonwebtoken": "^8.5.1",
    "jwks-rsa": "^1.6.0",
    "lodash": "^4.17.15",
    "moment": "^2.24.0",
    "mysql": "^2.17.1",
    "mysql2": "^1.7.0",
    "sequelize": "^5.18.4",
    "sequelize-cli": "^5.5.1",
    "serverless-dotenv-plugin": "^2.1.1",
    "serverless-offline": "^5.11.0",
    "uuid": "^3.3.3"
  },
  "optionalDependencies": {
    "win-node-env": "^0.4.0"
  }
}
